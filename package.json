{
  "scripts": {
    "layer0:dev": "layer0 dev",
    "layer0:build": "layer0 build",
    "layer0:deploy": "layer0 deploy"
  },
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web"
    ]
  },
  "devDependencies": {
    "@layer0/cli": "^4.0.1",
    "@layer0/core": "^4.0.1",
    "@layer0/devtools": "^4.0.1",
    "@layer0/prefetch": "^4.0.1",
    "@redwoodjs/core": "^0.38.0",
    "toml": "^3.0.0"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config"
  },
  "engines": {
    "node": ">=14.x <=16.x",
    "yarn": ">=1.15"
  },
  "prisma": {
    "seed": "yarn rw exec seed"
  }
}
